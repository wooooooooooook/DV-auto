name: Broadcast Today Links

on:
  schedule:
    - cron: '42 1 * * *' # 10:42 KST
  workflow_dispatch:

concurrency:
  group: telegram-bot
  cancel-in-progress: false

jobs:
  broadcast:
    if: ${{ vars.ENABLE_BROADCAST_TODAY_LINKS == 'true' }}
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      contents: read
    env:
      DV_USER: ${{ secrets.DV_USER }}
      DV_PASS: ${{ secrets.DV_PASS }}
      NOTICE_BOT_TOKEN: ${{ secrets.NOTICE_BOT_TOKEN }}
      NOTICE_CHANNEL_ID: ${{ secrets.NOTICE_CHANNEL_ID }}
      HEADLESS: 'true'
      TZ: Asia/Seoul

    steps:
      - uses: actions/checkout@v4

      - name: Enable Corepack (pnpm)
        run: corepack enable

      - name: Use repo pnpm version
        run: corepack prepare pnpm@10.24.0 --activate

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Cache Playwright Browsers
        id: cache-playwright-browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: ${{ runner.os }}-playwright-${{ hashFiles('**/pnpm-lock.yaml') }}-node20
          restore-keys: |
            ${{ runner.os }}-playwright-${{ hashFiles('**/pnpm-lock.yaml') }}
            ${{ runner.os }}-playwright-

      - name: Install Playwright Browsers
        run: pnpm exec playwright install --with-deps

      - name: Build
        run: pnpm run build

      - name: Broadcast today links to channel
        run: node dist/services/broadcast_today_links.js
