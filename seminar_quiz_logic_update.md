# 세미나 설문 참여 및 퀴즈 탐색 로직 업데이트 설명

이번 수정의 핵심은 **"설문참여" 버튼을 누른 후 뜨는 중간 창에서 다시 "참여하기" 버튼을 눌렀을 때 새롭게 나타나는 최종 팝업(실제 퀴즈창)을 정확하게 추적하여 화면이 완전히 열리기를 기다리도록 개선**한 것입니다.

전체 로직의 흐름은 다음과 같이 변경되었습니다.

## 1. 첫 번째 "설문참여" 버튼 클릭 및 1차 팝업 대기
1. 세미나 상세 화면에서 `text="설문참여"` 버튼을 찾습니다.
2. 이 버튼을 클릭하면서, 브라우저에서 새로운 창(팝업)이 열리는지 최대 5초간 대기합니다.
3. 팝업이 열렸다면 해당 팝업(`popupPage`)을 앞으로 사용할 퀴즈 탐색용 임시 메인 페이지(`quizPage`)로 지정합니다.
4. 만약 새 팝업이 뜨지 않았다면, 현재 페이지가 화면 이동을 한 것으로 간주하고 통신 로딩(`networkidle`)이 끝날 때까지 기다립니다.

## 2. "참여하기" / "설문 참여하기" 버튼 클릭 및 2차 팝업 처리 (★이번 수정 핵심)
1. 앞서 얻은 `quizPage` 안에서 `text="참여하기"` 또는 `text="설문 참여하기"` 버튼이 있는지 찾습니다.
2. 버튼이 보이면 클릭을 시도합니다. 이때 사용자님의 피드백("참여하기 버튼을 누르면 팝업이 뜨는 형태임")을 반영하여, **클릭과 동시에 새로운 팝업창(2차 팝업)이 열리는 이벤트를 무조건 감지**하도록 대기합니다.
3. 새로운 팝업(`secondPopup`)이 정상적으로 감지되면:
   - 메모리 확보를 위해 기존에 열려있던 1차 팝업 창을 닫아버립니다.
   - 새롭게 열린 이 2차 팝업을 최종 `quizPage` (실제 퀴즈가 들어있는 창)로 덮어씌우고 화면에 내용이 모두 렌더링될 때까지 기다립니다.
4. 만약 "참여하기"를 눌렀는데도 새로운 팝업이 뜨지 않고 현재 창 내부에서 화면만 전환되었다면, 현재 `quizPage`의 통신 로딩이 끝날 때까지(`networkidle`) 대기해줍니다.

## 3. 진입 성공 검증 (★새로 추가됨)
1. 최종 확정된 `quizPage` 안에서 (보내주신 스크린샷에 있던) **`"설문을 시작합니다"`**라는 텍스트를 가진 요소가 화면에 나타나는지 검사합니다.
2. 이 텍스트가 발견되지 않으면 콘솔에 `"설문을 시작합니다" 텍스트를 찾을 수 없음`이라는 로그를 남겨, 퀴즈 진입에 실패했음을 디버깅할 수 있게 해줍니다.

## 4. 퀴즈 파싱 및 정답 탐색 (기존 로직 유지)
1. 진입이 완료된 `quizPage`에서 `[퀴즈]`라는 텍스트가 포함된 영역들을 찾습니다.
2. 각 영역에서 문제 텍스트와 라디오 버튼(객관식 보기)들을 추출합니다.
3. 저장된 족보(`seminar_quiz_cheatsheet.json`) 키워드와 대조하여 정답을 찾아냅니다.

---

### 요약:
예전 코드에서는 "동의" 버튼을 누르도록 잘못 짜여 있었거나, "참여하기"를 누른 후 새롭게 뜨는 **최종 팝업창**을 제대로 기다리지 않았습니다. 그래서 **아직 퀴즈가 화면에 다 뜨지 않은(또는 뜨기 전의) 화면에서 퀴즈를 찾으려다 "퀴즈를 찾을 수 없습니다"라는 오류**를 내뱉고 실패했던 것입니다.

이번 수정을 통해 **`① 설문참여 클릭 -> ② 1차 팝업 추적 -> ③ 참여하기 클릭 -> ④ 2차 팝업 추적 대기 -> ⑤ '설문을 시작합니다' 텍스트 확인 -> ⑥ 퀴즈 정답 탐색`** 의 완벽한 순서로 팝업 흐름을 끝까지 쫓아가도록 개선되었습니다.
